[package]
name = "zkie-core"
version = "0.1.0"
edition = "2021"

[dependencies]
blake3 = "1"
halo2_proofs = { git = "https://github.com/privacy-scaling-explorations/halo2.git", tag = "v0.4.0", features = ["circuit-params"] }
rand_core = { version = "0.6", features = ["getrandom"] }
