[package]
name = "zkie-prover"
version = "0.1.0"
edition = "2021"

[dependencies]
blake3 = "1"
serde = { version = "1", features = ["derive", "rc"] }
thiserror = "1"
zkie-types = { path = "../zkie-types" }

[dev-dependencies]
blake3 = "1"
serde_json = "1"
