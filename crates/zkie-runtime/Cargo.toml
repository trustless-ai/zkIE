[package]
name = "zkie-runtime"
version = "0.1.0"
edition = "2021"

[dependencies]
blake3 = "1"
getrandom = "0.2"
rusqlite = { version = "0.40.2", features = ["bundled"] }
rustix = { version = "1", features = ["fs"] }
thiserror = "1"
zkie-types = { path = "../zkie-types" }
